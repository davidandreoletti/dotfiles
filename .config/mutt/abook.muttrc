set query_command = "abook --mutt-query '%s'"
macro generic,index,pager \ca "<shell-escape>abook<return>" "launch abook"
#macro index,pager gk "<pipe-message>abook --add-email<return>" "add the sender address to abook"

# vim: set ft=muttrc:
