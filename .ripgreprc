# Search also looks into git ignored files in:
#  --local .gitignore files
#  --$GIT_DIR/info/exclude
#  --core.excludesFile
#
# ripgrep search precedences in:
# - .ignore (highest priority)
# - .rcignore
# - .gitignore
#
# --no-ignore 
# -uuu

# Print the files that ripgrep would search, but don't actually search them
# --files 
 
# Search also looks into "dot" files
--hidden 
 
# Follow symlinked
--follow 

# Global Include/Exlude list
# - !{FOO,BAR} => exclude foo and bar
# - FOO => include foo
#-g "!.git"
#-g "!node_modules}/*"
