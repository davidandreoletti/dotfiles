# % rustup, run command with nightly toolchain
# # Run cargo with nightly toolchain
# ; usage: cargo_nightly <cargo command / args> 
# ; example: cargo_nightly build --release (same as nightly's cargo build --release) 
alias cargo_nightly='rustup run nightly cargo '

